const assert = require("assert");
const vscode = require("vscode");

const languageTest = require("../src/terminal");

suite("Extension Test Suite", () => {
  vscode.window.showInformationMessage("Start all tests.");

  test("", () => {});
});
